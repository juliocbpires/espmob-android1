<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProductApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground">@android:color/holo_blue_light</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/phone</item>
    </style>
    <style name="Theme.ProductApp" parent="Base.Theme.ProductApp" />
</resources>